server {
    server_name _;
    location / {
        rewrite ^ http://<%= node['againfm']['host'] %>? permanent;
    }

    location /nginx_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        deny all;
    }
}